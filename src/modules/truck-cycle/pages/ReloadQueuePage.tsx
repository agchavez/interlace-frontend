import { Box, Typography } from '@mui/material';

export default function ReloadQueuePage() {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h5" fontWeight={600}>Cola de Recarga</Typography>
            <Typography color="text.secondary" sx={{ mt: 1 }}>
                Esta funcionalidad estara disponible proximamente.
            </Typography>
        </Box>
    );
}
